**Date:** 12–16 May 2026

**What I worked on:** Rewrote the ROS2 package from Python to C++ — added `CMakeLists.txt`, an `ament_cmake` `package.xml`, and C++ skeletons for `core_loop`, `navigation`, and `pid_loop`. Spent several commits satisfying `uncrustify`/`ament` style checks (added `.clang-format`, moved braces to new lines, fixed `test_node.cpp`). Ported the PID controller logic into `pid_loop.cpp` (lifecycle-aware `on_configure`/`on_activate` wiring, PID computed from `rclcpp::Time` deltas).

**Reflection:** A run of "this should comply" → "braces are on new line now" → "actual uncrustify" commits shows how much of this week went into satisfying the linter before any new functional C++ logic was added — style compliance turned out to be its own small project.

**Decisions made:** Moved from Python to C++, for the performance headroom OpenCV image processing needs at the frame rates a line-follow robot requires; adopted ROS2's `ament`/`uncrustify` conventions from the start of the rewrite.

**Testing notes:**

**AI use log:**
