**Date:** 25–29 May 2026

**What I worked on:** Implemented short-edge removal and intersection merging in `pathfinding` (`removeShortEdges` deletes edges that touch an endpoint, and otherwise merges short edges into a neighbour via `mergeEdges`, which handles all four src/dst adjacency cases, reversing point chains where needed). Rewired the pathfinding step (`findNextNode`/`findPath`) to work on the new Edge/Graph model, using edge directions instead of raw node positions. In `line_follow`, added empty function-signature stubs to `navigation.hpp` ahead of writing real navigation logic.

**Reflection:** `mergeEdges` needing four separate symmetric cases (and point-chain reversal for two of them) was a good example of a "simple" operation, joining two edges, turning out to have more edge cases than expected once direction matters.

**Decisions made:** Chose to merge short edges into a neighbouring edge rather than simply deleting them, to avoid breaking graph connectivity at real (non-endpoint) intersections.

**Testing notes:**

**AI use log:**
