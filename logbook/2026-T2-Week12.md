**Date:** 17 Jul 2026

**What I worked on:** Added `rclcpp_components` and `nav_msgs` as declared dependencies in `package.xml`, alongside merging the accumulated `bains_simple_follow` branch into `main` via a pull request.

**Reflection:**

**Decisions made:**

**Testing notes:** The build was failing without these dependencies declared.

**AI use log:**
