**Date:** 23–24 Jul 2026

**What I worked on:** Added handling for breaks in the tracked line (e.g. from occlusion) via a search-based recovery mode, retuned tracking parameters (`edgePenalty` 0→20, `gatingThreshold` 10→50), fixed the cost-matrix bug from the week of 17–20 Jun (it was storing `penalty` instead of the combined `cost`), and added documentation comments to the tracking/update functions. Added a CTest-based integration test harness (`test_video.cpp`) that runs the full extraction pipeline against recorded footage as a smoke test.

**Reflection:** Writing documentation for `updateGraph` is what surfaced the month-old cost-matrix bug, a good example of writing docstrings as a forcing function to actually re-read code that had been treated as a black box for weeks.

**Decisions made:** Chose to test via CTest cases that run the real pipeline against pre-recorded video files rather than unit tests with synthetic input, trading isolation for closer-to-real-world coverage.

**Testing notes:** Added `add_test` entries in `CMakeLists.txt` running the extraction and tracking pipeline against four recorded videos (`test/rec.mp4`, `test/output*.mp4`), wired into `enable_testing()`, giving repeatable regression coverage against real footage.

**AI use log:**
