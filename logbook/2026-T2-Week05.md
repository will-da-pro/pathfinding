**Date:** 22–23 May 2026

**What I worked on:** Spent a chunk of this week getting a GitHub Action working that mirrors `line_follow` to a private assessment repository, cycling through a token-based push, a custom script, and finally an SSH deploy key, plus guard conditions so the workflow doesn't also run (or re-mirror) on the mirror repo itself. Separately, in `pathfinding`, refactored the line representation from a `map<Point, vector<Point>>` into proper `Node`/`Edge`/`Graph` structs with integer IDs, and started work on removing very short, noisy edges.

**Reflection:** The mirror-workflow session was real-time trial and error against GitHub's Action runner, landing eventually on an SSH deploy key plus a repository-name guard as the fix, after token-based approaches kept failing.

**Decisions made:** Rebuilt the pathfinding graph model around explicit `Node`/`Edge`/`Graph` structs instead of a point-keyed map, so edges become first-class objects that can carry metadata (length, endpoint flags) and be merged/compared, a foundation the rest of the project's tracking logic depends on.

**Testing notes:** The mirror-workflow debugging was effectively iterative testing of the CI/CD pipeline itself - pushing, watching the Action run, and adjusting.

**AI use log:**
